import 'package:flutter/material.dart';

class CampesinoHomeScreen extends StatelessWidget {
  const CampesinoHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Campesino Home')));
  }
}
